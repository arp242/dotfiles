#!/bin/sh
# 
# Rename all files in the current directory to lowercase
#

for f in ./*; do
	nf="$(echo "${f}" | tr '[:upper:]' '[:lower:]')"
	echo "mv -v \"${f}\" \"${nf}\""
	mv -v "${f}" "${nf}"
done
